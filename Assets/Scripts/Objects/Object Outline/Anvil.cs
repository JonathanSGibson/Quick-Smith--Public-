using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Anvil : InteractableObject
{
    //Jack's code
    new void Start()
    {
        base.Start(); //calls scripts from interactable object (no Anvil specific code currently)
    }

    new void Update()
    {
        base.Update(); //calls scripts from interactable object (no Anvil specific code currently)
    }
}
//End of Jack's code