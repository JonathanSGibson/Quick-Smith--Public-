using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Counter : InteractableObject
{
    //Jack's code
    
    // Start is called before the first frame update
    new private void Start()
    {
        base.Start();//calls scripts from interactable object (no Counter specific code currently)
    }

    // Update is called once per frame
    new private void Update()
    {
        base.Update();//calls scripts from interactable object (no Counter specific code currently)
    }
}
//End of Jack's code